# Teaching basic FEM

This repo uses Sympy to do symbolic manipulations, emulating what one would do if setting up and solving FEM by hand.

Obs: do `uv run jupyter nbconvert --to markdown single_element_fem.ipynb` to export notebook to markdown, if desired.